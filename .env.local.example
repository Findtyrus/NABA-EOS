NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
RESEND_API_KEY=
# Sender address for invite emails. Until you verify a domain in Resend,
# you must use onboarding@resend.dev and can only send to your own Resend account email.
RESEND_FROM_EMAIL=NABA <onboarding@resend.dev>
# Base URL used to build invite links (no trailing slash).
NEXT_PUBLIC_APP_URL=http://localhost:3000
